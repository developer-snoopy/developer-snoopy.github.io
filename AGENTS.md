# Project Guidelines & Agent Rules

## Git Workflow Automation
- After completing any requested code or document modifications, always automatically stage changes (`git add`), create a meaningful conventional commit (`git commit`), and push to the remote repository (`git push origin main`).
- Ensure the active GitHub account is `developer-snoopy` for repository operations.
